# Minimal requirements (ASCII only to avoid encoding issues)

# Core
# python >= 3.11 (not a pip package - handled by base image)

# Observer
pandas>=1.5.0
numpy>=1.24.0

# Logging
python-json-logger>=2.0.0

# API (optional)
# fastapi>=0.100.0
# uvicorn>=0.23.0
