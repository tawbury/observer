# Terraform 변수 - 개발(Dev) 환경

resource_group_name = "rg-observer-dev"
location           = "Korea South"

# 태그
tags = {
  Environment = "development"
  Project     = "qts-observer"
  ManagedBy   = "terraform"
  CostCenter  = "dev"
}

# 컴퓨트 설정 (필요시 확장)
# vm_size = "Standard_B1s"
# instance_count = 1
