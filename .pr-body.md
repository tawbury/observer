## 변경 요약
- **Observer 앱 Docker 리팩토링**: Dockerfile/.dockerignore를 `app/observer/`로 이동, 빌드 컨텍스트 `app/observer` 단일화
- **불필요 파일 제거**: `infra/docker/Dockerfile` 삭제, GHCR·compose 참조를 `app/observer/Dockerfile`로 변경
- **공통 App 템플릿 명세**: `docs/deployment/APP_DOCKER_TEMPLATE_SPECIFICATION.md` 추가 (QTS·신규 앱 기준)
- **배포 분석 문서**: `docs/deployment/GHCR_BUILD_AND_SERVER_STRUCTURE_ANALYSIS.md` (기존)

## 참고
- compose / 서버 구조는 이 PR에서 변경하지 않음
- 빌드: `docker build -f app/observer/Dockerfile app/observer`
