# Terraform 변수 - 프로덕션(Prod) 환경

resource_group_name = "rg-observer-prod"
location           = "Korea South"

# 태그
tags = {
  Environment = "production"
  Project     = "qts-observer"
  ManagedBy   = "terraform"
  CostCenter  = "production"
}

# 컴퓨트 설정 (필요시 확장)
# vm_size = "Standard_D2s_v3"
# instance_count = 3
