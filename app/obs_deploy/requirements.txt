# requirements.txt (권장 예시)
# 실제 필요한 패키지에 맞게 수정하세요.

# Core
# python >= 3.11 (not a pip package - use Dockerfile base image)

# Observer
pandas>=1.5.0
numpy>=1.24.0

# Logging
python-json-logger>=2.0.0

# API (향후)
# fastapi>=0.100.0
# uvicorn>=0.23.0
