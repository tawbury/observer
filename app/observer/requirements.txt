# Minimal requirements (ASCII only to avoid encoding issues)

# Core
# python >= 3.11 (not a pip package - handled by base image)

# Observer
pandas>=1.5.0
numpy>=1.24.0
psutil>=5.9.0

# Logging
python-json-logger>=2.0.0

# Database (Phase 13)
psycopg2-binary>=2.9.9
SQLAlchemy>=2.0.25
alembic>=1.13.1
asyncpg>=0.29.0
python-dotenv>=1.0.0

# API (optional)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# HTTP client for KIS REST
aiohttp>=3.9.0

# Symbol collection (manual script)
pykrx>=1.0.0
